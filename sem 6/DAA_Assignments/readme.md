## Assignment-1

1. Let A and B are two sorted lists such that A is in ascending order and B is in descending order. Write an algorithm to merge A and B such that the final list is in ascending order. Analyse and discuss the correctness of your algorithm.
2. Let A and B are two sorted lists such that A is in ascending order and B is in descending order. Write an algorithm to rearrange  A and B such that all the elements in A are less than or equal to all the elements in B. Analyse and discuss the correctness of your algorithm.
3. Let A be a list of unordered elements.Write an efficient algorithm to find min and maximum together with no of comparison 3n/2-2. Analyse and discuss the correctness of your algorithm.
4. Write a stable and inplace sorting algorithm. Analyse and discuss the correctness of your algorithm.
5. Write an algorithm to find the 2nd minimum/maximum element from an unsorted list. Analyse and discuss the correctness of your algorithm.

## Assignment-2

Take 3 sorted lists A, B and C. 
1. Write two way merging algorithms for merging the lists into a single sorted  list.
2. Analise the number of computations for all possible pairwise merging.
3. Discuss the correctness of your algorithms.

## Assignment-3
(Each Algorithm must have handwritten and have analysis and correctness proof)

1. Write an algorithm to check that if a given list of numbers are sorted.   
2. Consider the following list of numbers :  4,2,61,45,1,7,9,19,56,21,24. Write an algorithm to place 9 in its correct shorted position. 
3. Write an algorithm to place 9 in first location, followed by all numbers less than 9, followed by all numbers greater than 9. 
4. Let A and B are  32 digits  number.
- Write an algorithm to Add A and B
-  Write an algorithm to Multiply A and B.

## Assignment-4

Make a Slide with all discussion happened in class
(hint. 1. concept of  dynamic programming
          2. Fibinacci series with DP
          3. Knapsack Problem with DP.)
          

## Assignment-5

Make a presentation on the branch and bound technique to solve the 0/1 knapsack problem.


## Assignment-6

Make presentation on the KMP algorithm


## Assignment-7
(LSP Table (cont)/ P, NP,NP complete, NP hard)

Illustrate the following:

1. discuss an efficient  method to sort ten "10 digits" numbers  in almost constant time . (hint : Radix sort and from class discussion)
2. Convert Knapsack problem to satisfiability problem using state space tree. (hint: class discussion and reading [1] )
3. Generate the LSP or Pi Table for the following 
- aaaaaabbaa
- aaaa
- abcabcabc

4. State an example of each (except searching and sorting algorithm)
-  O(1) algorithm
- O(n) algorithm
- O(n^2) algorithm
- O(n^3) algorihtm
- O(log n) algorithm
- O(nlogn) algorithm
- O(2^n) algorithm
